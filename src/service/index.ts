import express from 'express'; // 載入 Express module
import path from 'path';

// Implicit Grant
          
// const app = express(); // 執行 Express 
// app.use(express.static(path.join(__dirname, 'public')));  // express.static() 輸出靜態檔，來源在public資料夾下的資料。
// app.listen(3001);

/*
    說明：
    - express.static(source) :輸出靜態檔
    - path.join() : 將所有參數拼成一組 url

*/